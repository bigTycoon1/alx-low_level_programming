#include "cops.h"
/**
* cops_mod - adds two given integers
* @a: first integer
* @b: second integer
*
* Return: sum of the two integers
*/
int cops_mod(int a, int b)
{
int results;
results = a % b;
return (results);
}
